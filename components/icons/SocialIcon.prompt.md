Brand social icon (Facebook/Instagram/LinkedIn/TikTok), loaded live from the Simple Icons CDN so the real mark is used — never reconstructed by hand.

```jsx
<SocialIcon network="instagram" color="FF3052" size={18} />
```

Used in the footer's social row, default color matches whatever the surface calls for (white on the dark footer, brand red as an accent option).
