/** Pearl-gray (#F8F8F8) full-width band used to alternate rhythm against pure-white sections — no border needed, contrast alone separates. */
export interface SectionSurfaceLightProps { children?: React.ReactNode; }
export function SectionSurfaceLight(props: SectionSurfaceLightProps): JSX.Element;
