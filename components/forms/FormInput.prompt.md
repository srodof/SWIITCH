Contact-form field: bold label + red required asterisk (unified to brand red — the live site's WPForms default is unmarked `#D63637`, corrected here), 43px flat input with the system's one visible hairline border.

```jsx
<FormInput label="Correo electrónico" type="email" />
```
